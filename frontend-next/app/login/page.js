import LoginClient from '../../components/LoginClient';

export const metadata = {
    title: 'লগইন | Titas - Dhaka University',
    description: 'তিতাস- তিতাস ম্যানেজমেন্ট সিস্টেম লগইন পেজ।',
};

export default function LoginPage() {
    return <LoginClient />;
}
